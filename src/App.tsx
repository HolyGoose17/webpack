import {
  AppBar,
  Box,
  Button,
  Card,
  Chip,
  Container,
  createTheme,
  Divider,
  IconButton,
  Stack,
  Typography,
} from "@mui/material";
import "./reset.css";
import { green, purple } from "@mui/material/colors";

export const theme = createTheme({
  palette: {
    primary: green,
    secondary: purple,
  },
});

function App() {
  return <></>;
}

export default App;
